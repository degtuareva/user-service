spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/edu_online_messenger_test
    username: test
    password: test
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
      show-sql: true
      properties:
        hibernate:
          format_sql: true
  liquibase:
    enabled: true
    change-log: classpath:changelog-master-test.xml
    drop-first: true

logging:
  level:
    root: INFO
    edu.online.messenger: DEBUG
    org.springframework: WARN